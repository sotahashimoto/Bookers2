<div class="container">
  <div class="row">

    <div class="col-xs-3">
      <%= render 'books/list', user: @user, book: @book %>
    </div>

    <div class="col-xs-9">
      <h1>Books</h1>

      <table>
        <tr>
          <th></th>
          <th></th>
          <th>Opinion</th>
        </tr>

        <% @loginuserdate.each do |book| %>
          <tr>
            <td><%= attachment_image_tag book.user, :profile_image, :fill, 100, 100, fallback:"no_image.jpg", size:"50×50" %></td>
            <td><%= link_to book.title, book_path(book.id) %></td>
            <td><%= book.body %></td>
          </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>