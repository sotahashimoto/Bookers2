<div class="container">
  <div class="row">

    <div class="col-xs-3">
      <%= render 'books/list', user: @user, book: @book %>
    </div>

    <div class="col-xs-9">
      <h1>Users</h1>
      <table>
        <tr>
          <th>image</th>
          <th>name</th>
          <th></th>
          <th></th>
        </tr>

        <% @users.each do |user| %>
          <tr>
            <td><%= attachment_image_tag user, :profile_image, :fill, 100, 100, fallback:"no_image.jpg", size:"50×50" %></td>
            <td><%= user.name %></td>
            <td><%= user.introduction %></td>
            <td><%= link_to "Show", user_path(user.id) %></td>
          </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>