<h1>User info</h1>

<% if book.errors.any? %>
  <h2><%= pluralize(book.errors.count, "error")%> prohibited this product from being saved:</h2>
  <% book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>

<table>
  <tr>
    <th></th><td><%= attachment_image_tag current_user, :profile_image, :fill, 100, 100, fallback:"no_image.jpg", size:"50×50" %></td>
  </tr>
  <tr>
    <th>name</th><td><%= current_user.name %></td>
  </tr>
  <tr>
    <th>introduction</th><td><%= current_user.introduction %></td>
  </tr>
</table>

<%= link_to edit_user_path(user) do %>
  <button type="button" class="btn col-xs-12 btn btn-default glyphicon glyphicon-wrench">
  </button>
<% end %>

<h2>New book</h2>
<%= form_for(@book) do |f| %>
  <h4>title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %><br />
  <%= f.submit 'Create Book' %>
<% end %>